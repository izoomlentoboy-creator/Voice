# EchoFlow 2.0 - Yandex Cloud Guide

**Полное руководство по обучению модели на Yandex Cloud с GPU**

---

## 💰 Стоимость

### GPU виртуальные машины

| Конфигурация | GPU | vCPU | RAM | Цена/час | 20 часов |
|--------------|-----|------|-----|----------|----------|
| **g1.1** (рекомендуется) | 1x Tesla V100 | 8 | 96 ГБ | ~₽50 | **~₽1000** |
| **g2.1** | 1x Tesla T4 | 8 | 48 ГБ | ~₽35 | ~₽700 |
| **g2.2** | 2x Tesla T4 | 16 | 96 ГБ | ~₽70 | ~₽1400 |

**Дополнительно:**
- Хранилище (100 ГБ SSD): ~₽8/месяц
- Исходящий трафик: ~₽1.5/ГБ (скачивание модели ~2 ГБ = ₽3)

**Итого для обучения: ~₽1000-1500**

---

## 🚀 Быстрый старт

### Предварительные требования

1. **Аккаунт Yandex Cloud**
   - Зарегистрируйтесь на https://cloud.yandex.ru
   - Активируйте пробный период (₽4000 на 60 дней)

2. **Установите Yandex CLI** (опционально)
   ```bash
   curl https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash
   ```

---

## 📋 Пошаговая инструкция

### Шаг 1: Создание виртуальной машины

#### Через веб-интерфейс

1. **Откройте консоль Yandex Cloud**
   - https://console.cloud.yandex.ru

2. **Создайте VM**
   - Compute Cloud → Виртуальные машины → Создать ВМ

3. **Выберите конфигурацию:**
   - **Зона доступности:** ru-central1-a
   - **Платформа:** GPU (Intel Broadwell with NVIDIA® Tesla® V100)
   - **Конфигурация:** g1.1 (1 GPU, 8 vCPU, 96 ГБ RAM)
   - **Образ:** Ubuntu 22.04 LTS
   - **Диск:** 100 ГБ SSD

4. **Настройте сеть:**
   - Публичный IP: Да
   - SSH ключ: Добавьте свой публичный ключ

5. **Создайте ВМ**
   - Нажмите "Создать ВМ"
   - Дождитесь запуска (~2 минуты)

#### Через CLI (быстрее)

```bash
# Создать VM с GPU
yc compute instance create \
  --name echoflow-training \
  --zone ru-central1-a \
  --platform gpu-standard-v1 \
  --cores 8 \
  --memory 96 \
  --gpus 1 \
  --network-interface subnet-name=default-ru-central1-a,nat-ip-version=ipv4 \
  --create-boot-disk image-folder-id=standard-images,image-family=ubuntu-2204-lts,size=100 \
  --ssh-key ~/.ssh/id_rsa.pub
```

---

### Шаг 2: Подключение к VM

1. **Получите IP адрес**
   - В консоли: Compute Cloud → Виртуальные машины → echoflow-training
   - Скопируйте публичный IP

2. **Подключитесь по SSH**
   ```bash
   ssh ubuntu@<PUBLIC_IP>
   ```

---

### Шаг 3: Автоматическая установка и обучение

**На виртуальной машине выполните:**

```bash
# Клонировать репозиторий
git clone https://github.com/izoomlentoboy-creator/Voice.git
cd Voice

# Запустить автоматическое обучение
./train_ultimate.sh
```

**Готово!** Скрипт автоматически:
- ✅ Установит CUDA и драйверы GPU
- ✅ Установит все зависимости
- ✅ Загрузит датасет (17.9 ГБ)
- ✅ Обучит модель (50 эпох, ~20 часов)
- ✅ Создаст deployment package

---

## 📊 Мониторинг обучения

### Вариант 1: Просмотр логов в реальном времени

```bash
# В SSH сессии
tail -f /tmp/training.log
```

### Вариант 2: Отключиться и вернуться позже

```bash
# Запустить в фоне
nohup ./train_ultimate.sh > training.log 2>&1 &

# Отключиться
exit

# Вернуться позже
ssh ubuntu@<PUBLIC_IP>
tail -f training.log
```

### Вариант 3: Использовать screen/tmux

```bash
# Установить screen
sudo apt-get install screen -y

# Создать сессию
screen -S training

# Запустить обучение
./train_ultimate.sh

# Отключиться (Ctrl+A, затем D)
# Вернуться позже
screen -r training
```

---

## 📈 Прогресс обучения

**Что вы увидите:**

```
======================================================================
  GPU Check
======================================================================
✓ GPU Available: Tesla V100-SXM2-32GB
✓ CUDA Version: 12.2
✓ GPU Memory: 32.0 GB

✅ Ready for training!
======================================================================

Epoch 1/50
Train Loss: 0.6234, Train Acc: 65.3%
Val Loss: 0.5891, Val Acc: 68.7%
Sensitivity: 66.2%, Specificity: 71.3%
Time: 24.5 min

Epoch 2/50
Train Loss: 0.5678, Train Acc: 72.1%
Val Loss: 0.5234, Val Acc: 74.8%
...
```

**Ожидаемое время:**
- Tesla V100: ~15-18 часов
- Tesla T4: ~20-24 часа

---

## 💾 Скачивание результатов

### После завершения обучения

```bash
# На VM создать архив
cd /home/ubuntu/Voice
tar -czf echoflow_deployment.tar.gz checkpoints/ *.log

# На вашем компьютере скачать
scp ubuntu@<PUBLIC_IP>:/home/ubuntu/Voice/echoflow_deployment.tar.gz ./
```

**Или используйте Yandex Object Storage (S3):**

```bash
# На VM установить AWS CLI
sudo apt-get install awscli -y

# Настроить (используйте ключи из консоли Yandex Cloud)
aws configure

# Загрузить в бакет
aws s3 cp echoflow_deployment.tar.gz s3://your-bucket/ --endpoint-url=https://storage.yandexcloud.net
```

---

## 🛑 Остановка и удаление VM

### ⚠️ ВАЖНО: Остановите VM после обучения!

**Иначе будет списываться оплата!**

### Через веб-интерфейс

1. Compute Cloud → Виртуальные машины
2. Выберите echoflow-training
3. Нажмите "Остановить" или "Удалить"

### Через CLI

```bash
# Остановить (сохранить диск)
yc compute instance stop echoflow-training

# Удалить полностью
yc compute instance delete echoflow-training
```

---

## 💡 Оптимизация затрат

### 1. Используйте прерываемые VM (скидка 50-70%)

**Прерываемые VM дешевле, но могут быть остановлены:**

```bash
yc compute instance create \
  --name echoflow-training \
  --preemptible \
  ... (остальные параметры)
```

**Цена:** ~₽15-25/час вместо ₽50/час

**Риск:** VM может быть остановлена через 24 часа
**Решение:** Используйте чекпоинты (автоматически сохраняются каждые 5 эпох)

### 2. Остановите VM на ночь

```bash
# Вечером остановить
yc compute instance stop echoflow-training

# Утром запустить
yc compute instance start echoflow-training

# Продолжить обучение
ssh ubuntu@<NEW_IP>
cd Voice
./train_ultimate.sh  # Продолжит с последнего чекпоинта
```

### 3. Используйте пробный период

**₽4000 бесплатно на 60 дней!**
- Хватит на 80 часов GPU V100
- Или 4 полных обучения модели

---

## 🔧 Решение проблем

### Проблема 1: "No GPU available"

**Причина:** Драйверы NVIDIA не установлены

**Решение:**
```bash
# Установить драйверы
sudo apt-get update
sudo apt-get install nvidia-driver-535 -y
sudo reboot

# После перезагрузки проверить
nvidia-smi
```

### Проблема 2: "CUDA out of memory"

**Причина:** Недостаточно GPU памяти

**Решение:**
```bash
# Уменьшить batch size в train.py
# Открыть файл
nano train.py

# Изменить BATCH_SIZE с 16 на 8
BATCH_SIZE = 8
```

### Проблема 3: "Dataset download failed"

**Причина:** Проблемы с сетью

**Решение:**
```bash
# Скачать вручную
wget https://zenodo.org/records/16874898/files/data.zip
unzip data.zip -d dataset/
```

### Проблема 4: "SSH connection lost"

**Причина:** Сетевые проблемы

**Решение:**
- Используйте screen/tmux (см. выше)
- Обучение продолжится в фоне
- Переподключитесь: `screen -r training`

---

## 📊 Сравнение с альтернативами

| Платформа | GPU | Цена | Время | Итого |
|-----------|-----|------|-------|-------|
| **Yandex Cloud V100** | Tesla V100 | ₽50/час | 18 часов | **₽900** |
| **Yandex Cloud T4** | Tesla T4 | ₽35/час | 24 часа | ₽840 |
| **Google Colab** | T4 | Бесплатно | 20 часов | ₽0 |
| **Kaggle** | T4 x2 | Бесплатно | 20 часов | ₽0 |
| **AWS p3.2xlarge** | Tesla V100 | $3/час | 18 часов | ~₽5000 |
| **Mac CPU** | Нет | Бесплатно | 200+ часов | ₽0 |

**Рекомендация:**
- **Бесплатно:** Google Colab или Kaggle
- **Быстро и недорого:** Yandex Cloud V100 (~₽900)
- **Максимальная скорость:** Yandex Cloud g2.2 (2x T4)

---

## 🎯 Полная стоимость проекта

### Минимальная конфигурация

- VM g2.1 (T4): ₽35/час × 24 часа = **₽840**
- Диск 100 ГБ: ₽8/месяц (пропорционально) = **₽0.27/день**
- Трафик: **₽3**
- **ИТОГО: ~₽850**

### Рекомендуемая конфигурация

- VM g1.1 (V100): ₽50/час × 18 часов = **₽900**
- Диск 100 ГБ: **₽0.24/день**
- Трафик: **₽3**
- **ИТОГО: ~₽910**

### С пробным периодом

- **₽0** (используйте ₽4000 из пробного периода)
- Останется ₽3000+ на другие эксперименты

---

## 📞 Поддержка

**Документация Yandex Cloud:**
- https://cloud.yandex.ru/docs/compute/

**Проблемы с кодом:**
- GitHub: https://github.com/izoomlentoboy-creator/Voice/issues

**Файлы:**
- `train_ultimate.sh` - Автоматическая установка и обучение
- `QUICKSTART.md` - Быстрый старт
- `VERIFICATION_CHECKLIST.md` - Чеклист проверки

---

## 🎉 Готово!

**Запустите обучение на Yandex Cloud за 5 минут!**

**Команды:**
```bash
# 1. Создать VM (через консоль или CLI)
# 2. Подключиться
ssh ubuntu@<PUBLIC_IP>

# 3. Запустить обучение
git clone https://github.com/izoomlentoboy-creator/Voice.git
cd Voice
./train_ultimate.sh

# 4. Дождаться завершения (~18 часов)
# 5. Скачать результаты
# 6. УДАЛИТЬ VM!
```

**Стоимость: ~₽900 (или бесплатно с пробным периодом)**

---

**Создано с ❤️ для диагностики патологий голоса**
