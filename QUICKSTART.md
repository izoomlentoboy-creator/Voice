# EchoFlow 2.0 - Quick Start Guide

**Запуск обучения одной командой - полностью автоматизировано!**

---

## 🚀 Быстрый старт (3 минуты)

### Вариант 1: Kaggle (Рекомендуется - БЕСПЛАТНО)

```bash
# 1. Создайте новый Kaggle Notebook с GPU T4 x2
# 2. Скопируйте и выполните:

!git clone https://github.com/izoomlentoboy-creator/Voice.git
%cd Voice
!bash train_ultimate.sh
```

**Готово!** Обучение запустится автоматически (~20 часов).

### Вариант 2: Локальный компьютер

```bash
git clone https://github.com/izoomlentoboy-creator/Voice.git
cd Voice
./train_ultimate.sh
```

---

## ✅ Что делает автоматический скрипт

**`train_ultimate.sh`** - это полностью автономная система, которая:

1. **Проверяет систему**
   - Определяет ОС, Python, GPU
   - Проверяет место на диске
   - Логирует все действия

2. **Устанавливает зависимости**
   - PyTorch (GPU или CPU версия)
   - Transformers, Librosa, SciPy
   - Все необходимые библиотеки
   - Автоматически исправляет ошибки

3. **Загружает датасет**
   - Saarbruecken Voice Database (17.9 ГБ)
   - С автоматическими повторными попытками
   - Распаковка и организация файлов

4. **Организует данные**
   - Разделяет на healthy/pathological
   - Создает train/val/test splits
   - Проверяет корректность

5. **Обучает модель**
   - 50 эпох с оптимальными параметрами
   - Автоматическое сохранение чекпоинтов
   - Отслеживание метрик

6. **Оценивает результаты**
   - Вычисляет accuracy, sensitivity, specificity
   - Сравнивает с целевыми значениями
   - Генерирует отчет

7. **Создает deployment package**
   - Упаковывает модель и метрики
   - Создает инструкции по развертыванию
   - Готовый к использованию пакет

---

## 🔍 Предварительная проверка (опционально)

Перед обучением можно запустить проверку:

```bash
python3 preflight_check.py
```

**Скрипт автоматически:**
- Проверит все зависимости
- Установит недостающие пакеты
- Протестирует модель
- Проверит forward pass
- Оценит готовность системы

**Результат:**
```
✓ Dependencies
✓ Model Files
✓ Model Import
✓ Forward Pass
✗ GPU (non-critical)
✓ Disk Space

Passed: 5/6
⚠ MOSTLY READY
```

---

## 📊 Мониторинг обучения

### Просмотр логов в реальном времени

```bash
tail -f /kaggle/working/training.log
```

### Проверка прогресса

```bash
# Посмотреть текущую эпоху
grep "Epoch" /kaggle/working/training.log | tail -5

# Посмотреть лучшую точность
grep "Best" /kaggle/working/training.log
```

### Проверка чекпоинтов

```bash
ls -lh /kaggle/working/checkpoints/
# best.pt - лучшая модель
# latest.pt - последний чекпоинт
# history.json - история обучения
```

---

## 📦 После обучения

### 1. Проверка результатов

```python
import json

with open('/kaggle/working/checkpoints/history.json', 'r') as f:
    history = json.load(f)

print(f"Best Validation Accuracy: {max(history['val_acc']):.2f}%")
```

### 2. Скачивание модели

**Kaggle:**
- Перейдите в Output → Download
- Скачайте `echoflow_v2_deployment.zip`

**Локально:**
```bash
# Модель находится в:
./checkpoints/best.pt
```

### 3. Развертывание

См. файл `DEPLOYMENT.md` в deployment package.

---

## 🛠️ Настройка параметров (опционально)

Отредактируйте `train_ultimate.sh`:

```bash
# Изменить количество эпох
EPOCHS=100  # по умолчанию 50

# Изменить batch size
BATCH_SIZE=32  # по умолчанию 16

# Изменить learning rate
LEARNING_RATE=0.0002  # по умолчанию 0.0001
```

---

## ❓ Решение проблем

### Проблема: "No space left on device"

**Решение:**
```bash
# Очистите кэш pip
rm -rf ~/.cache/pip

# Удалите временные файлы
rm -rf /tmp/*
```

### Проблема: "CUDA out of memory"

**Решение:** Уменьшите batch size в `train_ultimate.sh`:
```bash
BATCH_SIZE=8  # или даже 4
```

### Проблема: "Dataset download failed"

**Решение:** Скачайте вручную:
1. Откройте: https://zenodo.org/records/16874898/files/data.zip
2. Скачайте и распакуйте в `./dataset/`

### Проблема: "Import error: No module named X"

**Решение:** Запустите:
```bash
python3 preflight_check.py
# Скрипт автоматически установит недостающие пакеты
```

---

## 📈 Ожидаемые результаты

| Метрика | Целевое значение | Время достижения |
|---------|------------------|------------------|
| **Validation Accuracy** | 92-95% | Эпоха 30-40 |
| **Sensitivity** | 90-94% | Эпоха 35-45 |
| **Specificity** | 93-96% | Эпоха 30-40 |
| **F1-Score** | 0.91-0.93 | Эпоха 35-45 |

**Время обучения:**
- GPU T4: ~20 часов (50 эпох)
- CPU: ~200+ часов (НЕ рекомендуется)

---

## 🎯 Следующие шаги

1. **Запустите обучение** (одна команда)
2. **Дождитесь завершения** (~20 часов)
3. **Скачайте модель** (best.pt)
4. **Разверните на сервере** (см. DEPLOYMENT.md)
5. **Протестируйте на реальных данных**

---

## 💡 Советы

**Для Kaggle:**
- Используйте GPU T4 x2 (бесплатно 30 часов/неделю)
- Включите "Internet" в настройках ноутбука
- Сохраните Output после обучения

**Для локального обучения:**
- Минимум 16 ГБ RAM
- Минимум 25 ГБ свободного места
- GPU с 10+ ГБ VRAM (рекомендуется)

**Оптимизация:**
- Используйте SSD для датасета
- Закройте другие приложения
- Мониторьте температуру GPU

---

## 📞 Поддержка

**Проблемы с кодом:**
- GitHub Issues: https://github.com/izoomlentoboy-creator/Voice/issues

**Документация:**
- README.md - Полная документация
- DEPLOYMENT.md - Руководство по развертыванию
- EchoFlow_V2_Verification_Report.md - Отчет о проверке

---

**Готово! Запускайте обучение прямо сейчас! 🚀**

```bash
./train_ultimate.sh
```
