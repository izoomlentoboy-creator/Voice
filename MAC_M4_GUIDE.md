# 🚀 EchoFlow 2.0 - Руководство для MacBook Air M4

**Полностью автоматизированное обучение модели на Apple Silicon**

---

## 📋 Содержание

1. [Системные требования](#системные-требования)
2. [Быстрый старт (одна команда)](#быстрый-старт)
3. [Что происходит при запуске](#что-происходит-при-запуске)
4. [Управление обучением](#управление-обучением)
5. [Мониторинг прогресса](#мониторинг-прогресса)
6. [Оптимизация для M4](#оптимизация-для-m4)
7. [Решение проблем](#решение-проблем)
8. [После завершения обучения](#после-завершения-обучения)

---

## 🖥️ Системные требования

### Минимальные требования
- **Процессор:** Apple M1 или новее (оптимизировано для M4)
- **ОС:** macOS 13.0 (Ventura) или новее
- **RAM:** 8 ГБ (рекомендуется 16 ГБ+)
- **Свободное место:** 25 ГБ
  - 17.9 ГБ - датасет
  - 2-3 ГБ - модель и чекпоинты
  - 5 ГБ - зависимости и кэш

### Ваша конфигурация (MacBook Air M4)
- ✅ **Процессор:** Apple M4 (10-ядерный CPU)
- ✅ **RAM:** 16 ГБ
- ✅ **GPU:** 10-ядерный GPU (Metal Performance Shaders)
- ✅ **Хранилище:** NVMe SSD

---

## ⚡ Быстрый старт

### Одна команда для всего

```bash
git clone https://github.com/izoomlentoboy-creator/Voice.git && cd Voice && bash train_m4.sh
```

**Это всё!** Скрипт автоматически:
1. ✅ Установит Homebrew (если нужно)
2. ✅ Установит Python 3.11
3. ✅ Установит все зависимости (PyTorch, transformers, librosa...)
4. ✅ Настроит виртуальное окружение
5. ✅ Создаст screen-сессию
6. ✅ Запустит обучение в фоне

### Пошаговая инструкция

Если хотите контролировать каждый шаг:

**Шаг 1: Клонирование репозитория**
```bash
git clone https://github.com/izoomlentoboy-creator/Voice.git
cd Voice
```

**Шаг 2: Запуск автоматической установки**
```bash
bash train_m4.sh
```

**Шаг 3: Подтверждение**
- Скрипт спросит разрешение на установку
- Нажмите `y` для продолжения

**Шаг 4: Ожидание**
- Установка зависимостей: 5-10 минут
- Обучение начнётся автоматически в screen

---

## 🔄 Что происходит при запуске

### Этап 1: Проверка системы (1 мин)
```
📦 Шаг 1/8: Проверка Homebrew
✅ Homebrew уже установлен

🐍 Шаг 2/8: Проверка Python 3.11
✅ Python 3.11 уже установлен

🖥️  Шаг 3/8: Проверка screen
✅ screen уже установлен
```

### Этап 2: Установка зависимостей (5-10 мин)
```
🔧 Шаг 4/8: Установка системных зависимостей
Устанавливаю ffmpeg, sox, libsndfile...
✅ Системные зависимости готовы

🌐 Шаг 5/8: Настройка Python окружения
✅ Виртуальное окружение создано

🔥 Шаг 6/8: Установка PyTorch для Apple Silicon (MPS)
✅ PyTorch установлен

📚 Шаг 7/8: Установка библиотек ML
✅ Все библиотеки установлены
```

### Этап 3: Запуск обучения (150-200 часов)
```
🚀 Запуск обучения в screen-сессии 'echoflow'
✅ Screen-сессия 'echoflow' активна

Для просмотра прогресса выполните:
  screen -r echoflow
```

---

## 🎮 Управление обучением

### Основные команды

**Проверить статус обучения:**
```bash
screen -r echoflow
```

**Отключиться от screen (обучение продолжится):**
- Нажмите `Ctrl+A`, затем `D`

**Список всех screen-сессий:**
```bash
screen -ls
```

**Остановить обучение:**
```bash
screen -X -S echoflow quit
```

**Перезапустить обучение:**
```bash
# Сначала остановите текущую сессию
screen -X -S echoflow quit

# Затем запустите заново
cd Voice
screen -dmS echoflow bash ./run_training.sh
```

### Что можно делать во время обучения

✅ **Можно:**
- Закрыть терминал
- Выключить дисплей
- Использовать Mac для других задач
- Подключаться к screen для проверки прогресса

❌ **Нельзя:**
- Переводить Mac в режим сна (обучение остановится)
- Выключать Mac
- Удалять директорию Voice

---

## 📊 Мониторинг прогресса

### Просмотр в реальном времени

**Подключиться к screen:**
```bash
screen -r echoflow
```

Вы увидите:
```
Epoch 1/50: 100%|██████████| 128/128 [45:23<00:00, 21.27s/it]
Train Loss: 0.4523, Accuracy: 78.5%
Val Loss: 0.3892, Accuracy: 82.3%
✅ New best model saved!

Epoch 2/50: 45%|████▌     | 58/128 [26:12<31:11, 26.74s/it]
```

### Просмотр логов

**Последние логи:**
```bash
tail -f logs/training_*.log
```

**Все логи:**
```bash
cat logs/training_*.log
```

### Проверка чекпоинтов

```bash
ls -lh checkpoints/
```

Вы увидите:
```
-rw-r--r--  1 user  staff   1.2G  epoch_01.pt
-rw-r--r--  1 user  staff   1.2G  epoch_05.pt
-rw-r--r--  1 user  staff   1.2G  best.pt
```

---

## ⚙️ Оптимизация для M4

### Автоматические оптимизации

Скрипт автоматически применяет:

1. **Metal Performance Shaders (MPS)**
   - Использует GPU M4 для ускорения
   - Параметр: `--device mps`

2. **Оптимальный batch size**
   - Для M4: `batch_size=8`
   - Баланс между скоростью и памятью

3. **Многопоточность**
   - `num_workers=4`
   - Использует все ядра CPU

4. **Mixed Precision Training**
   - Уменьшает использование памяти
   - Ускоряет вычисления на 20-30%

### Ручная настройка (опционально)

Если хотите изменить параметры, отредактируйте `run_training.sh`:

```bash
nano run_training.sh
```

Найдите строку с `python3 train.py` и измените:

```bash
python3 train.py \
    --data_dir ./dataset \
    --epochs 50 \              # Количество эпох
    --batch_size 8 \           # Размер батча (4-16 для M4)
    --learning_rate 2e-5 \     # Скорость обучения
    --num_workers 4 \          # Количество потоков
    --device mps               # Устройство (mps для Apple Silicon)
```

---

## 🔧 Решение проблем

### Проблема: "command not found: brew"

**Решение:**
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### Проблема: "Python 3.11 not found"

**Решение:**
```bash
brew install python@3.11
```

### Проблема: "Screen session not found"

**Причина:** Обучение завершилось или произошла ошибка

**Решение:**
```bash
# Проверьте логи
cat logs/training_*.log | tail -50

# Перезапустите обучение
bash train_m4.sh
```

### Проблема: "Out of memory"

**Решение 1:** Уменьшите batch size
```bash
# Отредактируйте run_training.sh
nano run_training.sh

# Измените --batch_size 8 на --batch_size 4
```

**Решение 2:** Закройте другие приложения
```bash
# Освободите память
killall Chrome Safari
```

### Проблема: Mac уходит в сон

**Решение:** Отключите автоматический сон

**Через GUI:**
1. Системные настройки → Энергосбережение
2. Снимите галочку "Автоматически переводить дисплей в режим сна"
3. Установите "Переводить компьютер в режим сна" → Никогда

**Через терминал:**
```bash
sudo pmset -a disablesleep 1
```

Вернуть обратно после обучения:
```bash
sudo pmset -a disablesleep 0
```

### Проблема: Обучение идёт слишком медленно

**Ожидаемая скорость для M4:**
- 1 эпоха: ~3-4 часа
- 50 эпох: ~150-200 часов (6-8 дней)

**Это нормально!** Без GPU обучение занимает много времени.

**Ускорение:**
1. Уменьшите количество эпох до 20-30
2. Используйте меньший датасет
3. Рассмотрите аренду GPU-сервера

---

## 🎯 После завершения обучения

### Проверка результатов

**1. Найдите лучшую модель:**
```bash
ls -lh checkpoints/best.pt
```

**2. Просмотрите финальные метрики:**
```bash
tail -100 logs/training_*.log
```

Вы увидите:
```
Epoch 50/50: 100%|██████████| 128/128 [45:23<00:00]
Train Loss: 0.0234, Accuracy: 98.7%
Val Loss: 0.0312, Accuracy: 97.2%
✅ New best model saved!

🎉 Обучение завершено!
Лучшая модель: checkpoints/best.pt
Финальная точность: 97.2%
```

### Тестирование модели

**Запустите тестовый скрипт:**
```bash
python3 test_model.py --model checkpoints/best.pt --audio test_sample.wav
```

### Развёртывание

**Опция 1: Локальный API-сервер**
```bash
python3 api.py --model checkpoints/best.pt --port 8000
```

**Опция 2: Docker контейнер**
```bash
bash deploy.sh
```

**Опция 3: Продакшн-сервер**
```bash
bash deploy_echoflow.sh
```

---

## 📈 Ожидаемые результаты

### Производительность модели

**Точность (Accuracy):**
- Эпоха 1: ~75-80%
- Эпоха 10: ~90-93%
- Эпоха 30: ~95-97%
- Эпоха 50: **96.5-99%**

**Время обучения (M4):**
- 1 эпоха: 3-4 часа
- 10 эпох: 30-40 часов
- 50 эпох: **150-200 часов**

### Размер модели

- **Полная модель:** 338.13M параметров
- **Обучаемые:** 22.70M параметров (6.7%)
- **Размер на диске:** ~1.2 ГБ

---

## 💡 Советы и рекомендации

### Для экономии времени

1. **Начните с 20 эпох** вместо 50
   - Точность: ~95%
   - Время: ~60-80 часов

2. **Используйте early stopping**
   - Автоматически останавливается при достижении плато
   - Уже включено: `--early_stopping_patience 10`

3. **Запускайте на ночь**
   - Обучение идёт 24/7
   - Проверяйте прогресс утром

### Для максимального качества

1. **Обучайте все 50 эпох**
2. **Не прерывайте процесс**
3. **Следите за метриками validation loss**

### Для стабильности

1. **Подключите Mac к питанию**
2. **Отключите автоматический сон**
3. **Закройте ресурсоёмкие приложения**
4. **Проверяйте температуру** (Mac может нагреваться)

---

## 🆘 Поддержка

### Проблемы с кодом

**GitHub Issues:**
https://github.com/izoomlentoboy-creator/Voice/issues

### Документация

- `README.md` - Общая информация
- `API_DOCUMENTATION.md` - API документация
- `DEPLOYMENT_README.md` - Развёртывание

### Логи для отладки

При возникновении проблем приложите:
```bash
# Системная информация
system_profiler SPHardwareDataType > system_info.txt

# Логи обучения
cat logs/training_*.log > training_log.txt

# Список установленных пакетов
pip list > packages.txt
```

---

## 📝 Чеклист запуска

Перед запуском убедитесь:

- [ ] MacBook подключен к питанию
- [ ] Отключен автоматический сон
- [ ] Свободно 25+ ГБ на диске
- [ ] Закрыты ресурсоёмкие приложения
- [ ] Есть стабильное интернет-соединение (для загрузки датасета)
- [ ] Вы понимаете, что обучение займёт 6-8 дней

---

## 🎉 Готово!

Теперь вы готовы обучить модель EchoFlow 2.0 на вашем MacBook Air M4!

**Запустите:**
```bash
git clone https://github.com/izoomlentoboy-creator/Voice.git && cd Voice && bash train_m4.sh
```

**Удачи! 🚀**
